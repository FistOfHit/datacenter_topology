from setuptools import setup, find_packages

setup(
    name="topology_generator",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add dependencies here if known
    ],
    python_requires=">=3.6",
    description="Topology Generator Package",
    author="",
    author_email="",
)